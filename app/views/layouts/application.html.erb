<!DOCTYPE html>
<html>
<head>
  <title>Accountant</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id = "page">
    <% if user_signed_in?%>
    <div id = "header" >
       <div id = "signed_in">
            <p>Signed in as: </p>
            <h12><%= current_user.email %></h12>
            <p><%= link_to "Sign out", destroy_user_session_path, :method=>:delete%></p>
       </div>
       <div id = "admin_panel">
            <% if current_user.admin?%>
                <h2><%= link_to "Admin panel", admin_index_path %></h2>
                <h2><%= link_to "User panel", root_path %></h2>
            <% end %>
       </div>
       <%end%>
    </div>
  <div id = "notice">
       <%= flash[:notice]%>
  </div>
  <div id = "yield">
    <center><%= yield %> </center>
  </div>
</div>
</body>
</html>